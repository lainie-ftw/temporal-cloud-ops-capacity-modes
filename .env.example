# ----- Temporal Connection Settings -----
# These environment variables are automatically loaded by the Temporal Environment Config feature

# Your Temporal Cloud address (e.g., your-namespace.abc123.tmprl.cloud:7233)
TEMPORAL_ADDRESS=your-namespace.account.tmprl.cloud:7233

# The namespace where this automation workflow will run (format: namespace.account)
TEMPORAL_NAMESPACE=your-namespace.account

# Authentication Option 1: API Key (recommended for simplicity)
TEMPORAL_API_KEY=your-temporal-namespace-api-key

# Authentication Option 2: mTLS Certificates (alternative to API key)
# Uncomment these lines if using mTLS instead of API key
# TEMPORAL_TLS_CLIENT_CERT_PATH=/path/to/client-cert.pem
# TEMPORAL_TLS_CLIENT_KEY_PATH=/path/to/client-key.pem

# ----- Worker Settings -----
# Task queue name for the worker (default: capacity-management-task-queue)
TASK_QUEUE=capacity-management-task-queue

# ----- Cloud Metrics API Settings -----
# Your Temporal Cloud Metrics API key (Metrics Read-Only role)
TEMPORAL_CLOUD_METRICS_API_KEY=your-metrics-read-only-api-key

# Base URL for Cloud Metrics OpenMetrics API (default: https://metrics.temporal.io)
CLOUD_METRICS_API_BASE_URL=https://metrics.temporal.io

# ----- Cloud Ops API Settings -----
# Your Temporal Cloud Ops API key (get from cloud.temporal.io)
TEMPORAL_CLOUD_OPS_API_KEY=your-cloud-ops-api-key

# Base URL for Cloud Ops API (default: https://saas-api.tmprl.cloud)
CLOUD_OPS_API_BASE_URL=https://saas-api.tmprl.cloud

# ----- Operational Settings -----
# Enable dry run mode to preview changes without executing them (default: false)
DRY_RUN_MODE=false

# Comma-separated list of namespaces to manage (optional)
# If not set, all namespaces in the account will be managed
#NAMESPACE_ALLOWLIST=

# Comma-separated list of namespaces to exclude from management (optional)
#NAMESPACE_DENYLIST=

# Slack webhook URL to send notifications of failure or success to Slack
#SLACK_WEBHOOK_URL=
