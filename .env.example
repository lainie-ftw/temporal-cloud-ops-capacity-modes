# Temporal Connection Settings
# Your Temporal Cloud address (e.g., your-namespace.abc123.tmprl.cloud:7233)
TEMPORAL_ADDRESS=your-namespace.account.tmprl.cloud:7233

# The namespace where this automation workflow will run
TEMPORAL_NAMESPACE=your-namespace

# Authentication - Choose ONE of the following methods:
# Option 1: API Key Authentication (Recommended - simpler setup)
TEMPORAL_API_KEY=your-temporal-namespace-api-key

# Option 2: mTLS Certificate Authentication (Alternative)
# Uncomment and use these if you prefer mTLS instead of API key
# TEMPORAL_CERT_PATH=/path/to/your/client.pem
# TEMPORAL_KEY_PATH=/path/to/your/client-key.pem

### Metrics API Settings ###
TEMPORAL_CLOUD_METRICS_API_KEY=your-metrics-read-only-key
CLOUD_METRICS_BASE_URL=https://metrics.temporal.io/v1/metrics 

### Cloud Ops API Settings ###
# Your Temporal Cloud Ops API key (get from cloud.temporal.io)
TEMPORAL_CLOUD_OPS_API_KEY=your-cloud-ops-api-key

# Base URL for Cloud Ops API (default: https://saas-api.tmprl.cloud)
CLOUD_OPS_API_BASE_URL=https://saas-api.tmprl.cloud

# Capacity Management Settings
# Number of TRUs to enable when turning on provisioned capacity (default: 5)
DEFAULT_TRU_COUNT=5

# Minimum actions per hour to keep provisioned capacity enabled (default: 100)
MIN_ACTIONS_THRESHOLD=100

# Notification Settings
# Slack webhook URL for failure notifications (optional)
# Leave empty or comment out to disable Slack notifications
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL

# Operational Settings
# Enable dry run mode to preview changes without executing them (default: false)
DRY_RUN_MODE=false

# Comma-separated list of namespaces to manage (optional)
# If empty or not set, all namespaces in the account will be managed
NAMESPACE_ALLOWLIST=

# Comma-separated list of namespaces to exclude from management (optional)
NAMESPACE_DENYLIST=

# Worker Settings
# Task queue name for the worker (default: capacity-management-task-queue)
TASK_QUEUE=capacity-management-task-queue
